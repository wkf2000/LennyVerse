services:
  api:
    build:
      context: .
    ports:
      - "${APP_PORT:-8000}:8000"
    env_file:
      - .env
    volumes:
      - ./backend:/app/backend
      - ./data-pipeline:/app/data-pipeline
      - ./data:/app/data
